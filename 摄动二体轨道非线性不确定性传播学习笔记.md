# 基于自适应高斯混合的摄动二体轨道非线性不确定性传播学习笔记

> **论文**：*Nonlinear Uncertainty Propagation for Perturbed Two-Body Orbits*  
> **期刊**：Journal of Guidance, Control, and Dynamics, Vol. 37, No. 5, 2014, pp. 1415-1425  
> **DOI**：[10.2514/1.G000472](https://doi.org/10.2514/1.G000472)  
> **作者**：Kumar Vishwajeet, Puneet Singla, Moriba Jah  
> **原文 PDF**：[`Nonlinear Uncertainty Propagation for Perturbed Two-Body Orbits.pdf`](Nonlinear%20Uncertainty%20Propagation%20for%20Perturbed%20Two-Body%20Orbits.pdf)  
>
> **阅读说明**：GitHub 行内公式采用 `` $`...`$ ``；块级公式采用独立的 `` ```math `` 代码块，且每个公式写在同一行。本文中的 AGMM 指 Adaptive Gaussian Mixture Model，加入量测更新后称为 AGSF（Adaptive Gaussian Sum Filter）。

---

## 一句话主旨

作者用多个局部高斯核表示轨道状态概率密度，以无迹变换传播每个核的均值和协方差，再利用 Fokker-Planck/Chapman-Kolmogorov 方程持续重估混合权重，从而在稀疏观测、长时间传播以及 $`J_2`$ 与大气阻力摄动下保留偏斜、弯曲和多峰等非高斯特征。

---

## 一、论文要解决什么问题？

轨道确定通常用均值和协方差描述状态不确定性，并隐含假设状态满足单一高斯分布。对于短时、弱非线性传播，这一近似可能足够；但经过多圈传播后，轨道动力学会使概率密度逐渐弯曲、偏斜甚至产生长尾。

传统方法存在以下问题：

1. **EKF**：对动力学和量测模型作一阶线性化，容易低估强非线性下的协方差；
2. **UKF**：避免 Jacobian，但仍用单一均值和协方差表示完整概率密度；
3. **普通 GSF**：可以表达非高斯分布，但两次观测之间通常保持各分量权重不变；
4. **粒子滤波**：表达能力强，但在六维轨道状态中可能需要大量粒子；
5. **网格法求解概率密度方程**：受到维数灾难和无界状态空间的限制。

因此，论文关心的不只是“均值和协方差传播是否准确”，而是：

> 如何以有限数量的参数化高斯核，近似求解完整轨道状态概率密度的时间演化？

---

## 二、摄动二体轨道模型

卫星状态写成：

```math
\boldsymbol{x}=\begin{bmatrix}x&y&z&\dot{x}&\dot{y}&\dot{z}\end{bmatrix}^{\mathsf T}
```

位置满足：

```math
\ddot{\boldsymbol r}=-\frac{\mu}{r^3}\boldsymbol r+\boldsymbol a_{J_2}+\boldsymbol a_D
```

其中第一项是中心引力，$`\boldsymbol a_{J_2}`$ 表示地球非球形摄动，$`\boldsymbol a_D`$ 表示大气阻力。

### 1. $`J_2`$ 摄动

论文采用的 $`J_2`$ 加速度可以紧凑地写成：

```math
\boldsymbol a_{J_2}=-\frac{3}{2}J_2\frac{\mu R_e^2}{r^5}\begin{bmatrix}x\left(1-5z^2/r^2\right)\\y\left(1-5z^2/r^2\right)\\z\left(3-5z^2/r^2\right)\end{bmatrix}
```

### 2. 大气阻力

相对大气速度为：

```math
\boldsymbol v_{\mathrm{rel}}=\begin{bmatrix}\dot{x}+\Omega_e y&\dot{y}-\Omega_e x&\dot{z}\end{bmatrix}^{\mathsf T}
```

阻力模型为：

```math
\boldsymbol a_D=-\frac{1}{2}B\rho\left\|\boldsymbol v_{\mathrm{rel}}\right\|\boldsymbol v_{\mathrm{rel}}
```

大气密度使用指数模型：

```math
\rho=\rho_0\exp\left[-\frac{(r-R_e)-h_0}{H}\right]
```

理论模型允许加入随机过程噪声，但本文数值仿真假设系统动力学完全已知，没有过程噪声，只传播初始状态不确定性。

---

## 三、从状态轨迹转向概率密度演化

连续随机系统写成：

```math
\dot{\boldsymbol x}(t)=\boldsymbol f(t,\boldsymbol x(t))+\boldsymbol g(t,\boldsymbol x(t))\boldsymbol\eta(t)
```

离散形式为：

```math
\boldsymbol x_{k+1}=\boldsymbol\phi(k,\boldsymbol x_k)+\boldsymbol\eta_{k+1}
```

论文把研究对象从单条确定轨道 $`\boldsymbol x(t)`$ 提升为状态概率密度 $`p(t,\boldsymbol x)`$。

### 1. 连续时间：Fokker-Planck-Kolmogorov 方程

```math
\frac{\partial p}{\partial t}=-\sum_{i=1}^{n}\frac{\partial}{\partial x_i}(f_i p)+\frac{1}{2}\sum_{i=1}^{n}\sum_{j=1}^{n}\frac{\partial^2}{\partial x_i\partial x_j}\left([\boldsymbol g\boldsymbol Q\boldsymbol g^{\mathsf T}]_{ij}p\right)
```

### 2. 离散时间：Chapman-Kolmogorov 方程

```math
p(t_{k+1},\boldsymbol x_{k+1})=\int p(t_{k+1},\boldsymbol x_{k+1}\mid t_k,\boldsymbol x_k)p(t_k,\boldsymbol x_k)\,d\boldsymbol x_k
```

直接求解这些方程很困难，因为概率密度必须同时满足：

- $`p(\boldsymbol x)\ge 0`$；
- $`\int p(\boldsymbol x)d\boldsymbol x=1`$；
- 状态空间通常无界；
- 六维空间网格会产生维数灾难。

---

## 四、用自适应 GMM 表示状态概率密度

论文用有限个高斯核近似状态概率密度：

```math
\hat p(t_k,\boldsymbol x_k)=\sum_{i=1}^{N}w_k^i\,\mathcal N(\boldsymbol x_k;\boldsymbol\mu_k^i,\boldsymbol P_k^i)
```

权重满足：

```math
w_k^i\ge 0,\qquad \sum_{i=1}^{N}w_k^i=1
```

每个高斯分量由三部分描述：

- 均值 $`\boldsymbol\mu_k^i`$：该局部轨道假设的中心；
- 协方差 $`\boldsymbol P_k^i`$：该局部假设的不确定性形状；
- 权重 $`w_k^i`$：该高斯核在全局密度近似中的系数。

算法把大规模概率密度传播拆成两类问题：

1. 分别传播多个小的局部高斯分量；
2. 求解一个带概率约束的权重优化问题。

---

## 五、用无迹变换传播每个高斯分量

对于第 $`i`$ 个高斯分量：

```math
\mathcal N(\boldsymbol x_k;\boldsymbol\mu_k^i,\boldsymbol P_k^i)
```

先根据均值和协方差生成 sigma 点 $`\boldsymbol\chi_k^{i,j}`$，再调用完整非线性轨道传播器：

```math
\boldsymbol\chi_{k+1}^{i,j}=\boldsymbol\phi(k,\boldsymbol\chi_k^{i,j})
```

传播后的均值和协方差为：

```math
\boldsymbol\mu_{k+1}^i=\sum_jW_j^{(m)}\boldsymbol\chi_{k+1}^{i,j}
```

```math
\boldsymbol P_{k+1}^i=\sum_jW_j^{(c)}(\boldsymbol\chi_{k+1}^{i,j}-\boldsymbol\mu_{k+1}^i)(\boldsymbol\chi_{k+1}^{i,j}-\boldsymbol\mu_{k+1}^i)^{\mathsf T}+\boldsymbol Q_k
```

这样不需要显式计算动力学 Jacobian。

论文所说的“无需显式知道系统动力学”应理解为：

> 不需要写出解析 Jacobian，但仍需要一个可以将给定状态传播到下一时刻的黑箱数值模型。

对于 CKE 加无迹变换的离散形式，整个传播和积分过程可以做到导数自由；连续 FPKE 形式则仍需要使用相应的微分算子信息。

---

## 六、为什么传播过程中还要更新权重？

传统 GSF 在没有观测时通常令：

```math
w_{k+1}^i=w_k^i
```

但在非线性映射下，一个高斯分量的真实传播结果一般不再是高斯。UKF 只用新的均值和协方差重新拟合它，局部形状误差会随时间累积。

AGMM 因此将 FPKE/CKE 残差作为反馈，在传播期间重新计算权重：

```math
\boldsymbol w_k\longrightarrow\boldsymbol w_{k+1}
```

这一点需要谨慎理解：

> 若每个分量的密度都能被精确传播，那么确定性动力学不会改变该分量携带的概率质量。本文改变权重，是为了补偿“用单个高斯重新逼近每个非高斯传播结果”产生的表示误差，而不是说真实概率质量在不同物理轨道之间转移。

---

## 七、由 CKE 残差得到凸二次规划

传播后，作者寻找一组新权重，使高斯混合尽量逼近 CKE 给出的目标概率密度。对应的优化问题是：

```math
\min_{\boldsymbol w_{k+1}}\quad J=\frac{1}{2}\boldsymbol w_{k+1}^{\mathsf T}\boldsymbol M\boldsymbol w_{k+1}-\boldsymbol w_{k+1}^{\mathsf T}\boldsymbol N\boldsymbol w_k
```

约束为：

```math
\boldsymbol 1^{\mathsf T}\boldsymbol w_{k+1}=1,\qquad \boldsymbol w_{k+1}\ge\boldsymbol 0
```

其中，矩阵 $`\boldsymbol M`$ 描述新时刻高斯核之间的重叠：

```math
m_{ij}=\left|2\pi(\boldsymbol P_{k+1}^i+\boldsymbol P_{k+1}^j)\right|^{-1/2}\exp\left[-\frac{1}{2}(\boldsymbol\mu_{k+1}^i-\boldsymbol\mu_{k+1}^j)^{\mathsf T}(\boldsymbol P_{k+1}^i+\boldsymbol P_{k+1}^j)^{-1}(\boldsymbol\mu_{k+1}^i-\boldsymbol\mu_{k+1}^j)\right]
```

矩阵 $`\boldsymbol N`$ 的元素为：

```math
n_{ij}=\int\mathcal N(\boldsymbol\phi(k,\boldsymbol x_k);\boldsymbol\mu_{k+1}^i,\boldsymbol P_{k+1}^i+\boldsymbol Q_k)\mathcal N(\boldsymbol x_k;\boldsymbol\mu_k^j,\boldsymbol P_k^j)\,d\boldsymbol x_k
```

可以将其理解为：

- $`\boldsymbol M`$ 衡量候选新混合密度自身的重叠；
- $`\boldsymbol N\boldsymbol w_k`$ 衡量新高斯基函数与真实传播目标的重叠；
- 最小化目标函数相当于减小两者之间的平方积分误差；
- 非负和归一化约束保证最终结果仍然是合法概率密度。

这是一个凸二次规划，因此不会陷入一般非凸混合模型优化中的局部极小问题。

---

## 八、矩阵 $`\boldsymbol N`$ 的数值积分

$`m_{ij}`$ 可以解析计算，而 $`n_{ij}`$ 通常必须数值积分。论文将积分写成高斯期望：

```math
n_{ij}=\mathbb E_{\boldsymbol x_k\sim\mathcal N(\boldsymbol\mu_k^j,\boldsymbol P_k^j)}\left[\mathcal N(\boldsymbol\phi(k,\boldsymbol x_k);\boldsymbol\mu_{k+1}^i,\boldsymbol P_{k+1}^i+\boldsymbol Q_k)\right]
```

再用求积点近似：

```math
n_{ij}\approx\sum_{q=1}^{n_p}W_q\,g(\boldsymbol\chi_k^{j,q})
```

对于六维状态：

- 每维 2 点的 Gauss-Hermite 规则需要 $`2^6=64`$ 个点；
- 每维 3 点需要 $`3^6=729`$ 个点；
- $`2n+1`$ 无迹点只需要 13 个点；
- $`6n+1`$ 无迹点需要 37 个点。

论文表 1 中的 Frobenius 范数显示，37 点规则与 64/729 点 Gauss-Hermite 结果一致，而 13 点规则误差明显。正文还声称 simplex 点能够以更少点数取得准确结果，但早期版本的表 1 没有列出 simplex 对应行，阅读时应注意这一正文与表格之间的不完整对应。

---

## 九、计算复杂度与并行化

设：

- 状态维数为 $`n`$；
- 高斯分量数为 $`N`$；
- 每个积分使用 $`L`$ 个 sigma 点；
- 二次规划输入长度为 $`S`$。

论文给出的单次预测总复杂度为：

```math
O\left(n^3+N^2n^3+N^2L+SN^5\right)
```

主要计算瓶颈不是单个 UKF，而是 $`N\times N`$ 矩阵的积分计算以及随分量数快速增长的权重优化。

论文用 MPI 对 $`\boldsymbol N`$ 矩阵按行分配：第 $`r`$ 个进程计算第 $`r,r+P,r+2P,\ldots`$ 行，最后汇总到主进程。作者没有盲目并行全部代码，因为线程同步和通信本身也会产生开销。

---

## 十、数值仿真设置

论文考虑一颗高度约 622 km 的近地轨道卫星，包含 $`J_2`$ 和大气阻力摄动。

初始均值为：

```math
\boldsymbol\mu_0=\begin{bmatrix}7000&0&0&0&-1.0374&7.4771\end{bmatrix}^{\mathsf T}
```

其中位置单位为 km，速度单位为 km/s。

初始协方差为：

```math
\boldsymbol P_0=\operatorname{diag}(0.01,0.01,0.01,10^{-6},10^{-6},10^{-6})
```

即：

- 三个位置方向标准差均为 100 m；
- 三个速度方向标准差均为 1 m/s。

其他设置：

- 轨道周期约 0.8104 h；
- 总传播时间为 6 h；
- Monte Carlo 基准使用 125,000 个样本；
- 在原始高斯之外预置 48 个零权重分量，共 49 个分量；
- 新分量沿初始协方差主轴布置；
- 每圈轨道更新权重 100 次；
- $`\boldsymbol N`$ 矩阵尺寸为 $`49\times49`$。

初始时只有原始分量权重为 1，其余 48 个分量权重为 0，因此初始混合密度仍与单高斯完全相同。传播后，凸优化可以给这些预置分量分配非零权重，从而增加密度表示能力。

---

## 十一、无观测传播结果

图 1 表明，AGMM 计算的均值落在 Monte Carlo 样本均值的收敛误差范围内。

图 2 和图 3 比较了：

- Monte Carlo；
- AGMM-FPKE；
- AGMM-CKE；
- UKF；
- EKF。

传播约 1.61 h 后，概率密度开始出现明显弯曲；传播至 3.2、4 和 5.6585 h 后，单一椭圆已难以描述真实分布。

主要现象是：

1. EKF/UKF 只能给出单一椭圆轮廓；
2. AGMM-FPKE 和 AGMM-CKE 能形成弯曲、拉伸的密度轮廓；
3. AGMM 结果总体更接近 Monte Carlo 点云；
4. 图 4 中，AGMM 能跟踪 $`x`$ 和 $`z`$ 方向的偏度，而单高斯方法的偏度恒为零。

因此，本文优势不仅是均值更准确，更重要的是保留了高阶形状信息。

---

## 十二、加入观测后的 AGSF

量测模型为：

```math
\boldsymbol z_k=\boldsymbol h(t_k,\boldsymbol x_k)+\boldsymbol\nu_k,\qquad \boldsymbol\nu_k\sim\mathcal N(\boldsymbol 0,\boldsymbol R_k)
```

观测到来时，对每个高斯分量分别执行 EKF 或 UKF 型更新，再根据该分量的观测似然调整权重：

```math
w_{k+1\mid k+1}^i=\frac{w_{k+1\mid k}^i\gamma_i}{\sum_{j=1}^{N}w_{k+1\mid k}^j\gamma_j}
```

其中 $`\gamma_i`$ 是第 $`i`$ 个分量产生实际观测的预测似然。

这一步和传播期权重优化的作用不同：

- **传播期权重更新**：以 FPKE/CKE 残差为依据，修正概率密度表示误差；
- **量测期权重更新**：以观测似然为依据，执行贝叶斯假设概率更新。

如果去掉传播期间的 FPKE/CKE 权重反馈，AGSF 就退化为普通 GSF。

---

## 十三、三种观测场景

地面站位于美国科罗拉多州 Colorado Springs 附近：

```math
\boldsymbol r_{\mathrm{site}}=\begin{bmatrix}-1275.1219&-4797.9890&3994.2975\end{bmatrix}^{\mathsf T}\ \mathrm{km}
```

观测时刻为传播后的 5.6585 h。作者选择六组观测，其中四组位于先验密度尾部，两组位于先验均值或其附近，以测试滤波器在低概率观测下是否会过度自信。

### 场景 I：直接观测 $`x,y,z`$

```math
\boldsymbol z=\begin{bmatrix}x&y&z\end{bmatrix}^{\mathsf T}+\boldsymbol\nu,\qquad \boldsymbol R_1=\operatorname{diag}(0.01,0.01,0.01)\ \mathrm{km}^2
```

表 2 对位于先验均值之外的观测给出：

- AGSF：位置误差 0.237 km，位置方差 0.7837 km$`^2`$；
- UKF：位置误差 0.244 km，位置方差 0.0339 km$`^2`$；
- EKF：位置误差 1.3329 km，位置方差 0.03194 km$`^2`$。

EKF 的误差最大，但报告的方差反而最小，表现为明显的统计不一致。

### 场景 II：距离、方位角和俯仰角

```math
\rho=\left\|\boldsymbol r-\boldsymbol r_{\mathrm{site}}\right\|+\nu_1
```

角度由站心相对位置的反正切关系得到，量测噪声协方差为：

```math
\boldsymbol R_2=\operatorname{diag}(0.01\ \mathrm{km}^2,0.0174\ \mathrm{rad}^2,0.0174\ \mathrm{rad}^2)
```

表 3 给出：

- AGSF：位置误差 2.042 km，位置方差 3.4608 km$`^2`$；
- UKF：位置误差 7.1679 km，位置方差 3.1315 km$`^2`$；
- EKF：位置误差 7.7479 km，位置方差 0.3987 km$`^2`$。

AGSF 的均值误差明显更小，并且图 8 中可保留多峰后验。EKF 不仅均值误差大，而且严重低估协方差。

### 场景 III：仅方位角和俯仰角

量测噪声协方差为：

```math
\boldsymbol R_3=\operatorname{diag}(0.0174,0.0174)\ \mathrm{rad}^2
```

表 4 给出：

- AGSF：位置误差 469.863 km，位置方差 8264 km$`^2`$；
- UKF：位置误差 469.944 km，位置方差 8439 km$`^2`$；
- EKF：位置误差 469.947 km，位置方差 8436 km$`^2`$。

三种方法的均值误差接近，说明单次角度观测的距离可观测性很弱。AGSF 的主要优势不在于均值误差大幅降低，而在于真实状态始终落在其 99% 后验轮廓内，不确定性描述更可信。

---

## 十四、本文到底有没有“自动分裂”？

没有。

本文在仿真开始时预先加入 48 个零权重高斯分量，之后通过权重优化激活或抑制这些分量。它没有根据局部非线性在线创建新的子分量，也没有自动选择分裂方向或分裂次数。

与仓库中的 [`Automated Splitting Gaussian Mixture学习笔记.md`](Automated%20Splitting%20Gaussian%20Mixture%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0.md) 对比：

- 本文重点是**传播过程中重估已有分量的权重**；
- Automated Splitting 论文重点是**量测更新前根据非线性主动分裂分量**；
- 两者可以互补：传播阶段使用 AGMM 权重校正，量测阶段使用自动分裂提高非线性观测更新精度。

这也回答了一个常见疑问：

> 本文确实在无观测传播期间进行自适应处理，但自适应的是权重，不是分量数量。

---

## 十五、算法伪代码

```text
输入：初始 GMM、轨道传播器、观测序列、过程噪声与量测噪声

for 每个传播步 k:
    for 每个高斯分量 i:
        生成 sigma 点
        调用非线性轨道模型传播 sigma 点
        重构 μ[k+1,i] 和 P[k+1,i]

    计算高斯核重叠矩阵 M
    用 sigma 点/求积点计算传播交叉矩阵 N
    求解带 w>=0、sum(w)=1 约束的凸二次规划
    得到传播后的权重 w[k+1|k]

    if 时刻 k+1 有观测:
        对每个分量执行 EKF/UKF 量测更新
        计算各分量观测似然 gamma[i]
        按 Bayes 公式更新并归一化权重

输出：各时刻的 GMM 状态概率密度
```

---

## 十六、核心认识

1. **均值和协方差正确不代表概率密度正确**。碰撞概率、尾部风险和多假设决策依赖完整分布形状。
2. **UT 负责局部传播，Kolmogorov 方程负责全局密度校正**。两者解决的问题不同。
3. **权重变化是表示层修正**。它主要补偿局部高斯传播的形状误差。
4. **传播权重和量测权重有不同的信息来源**。前者来自概率密度演化方程，后者来自观测似然。
5. **AGMM 避免状态空间网格，但没有消除高维困难**。计算压力转移到了分量两两积分和权重优化。
6. **弱可观测性不能靠滤波算法凭空消除**。仅有角度观测时，三种方法均有数百千米位置误差。
7. **可信协方差比表面上更小的协方差重要**。EKF 在尾部观测下可能误差很大却给出很小方差。

---

## 十七、局限与可继续研究的问题

1. 分量数量和初始位置需要人工预设；
2. 预置零权重分量在高维空间中如何有效布置没有普适答案；
3. $`N^2`$ 个积分项以及二次规划使大规模分量计算昂贵；
4. 本文仿真没有引入动力学模型误差和过程噪声；
5. 大气密度和阻力系数在真实任务中具有显著不确定性；
6. 缺少自动剪枝、合并、分裂和误差控制机制；
7. 主要通过单个算例和二维投影轮廓验证，尚不足以证明所有轨道区域的鲁棒性；
8. 论文没有给出 AGMM 近似误差随分量数、积分阶数和传播时间变化的严格上界；
9. 表 1 对 simplex 求积的正文结论与表格呈现不完全一致，需要结合后续文献核查；
10. 工程实现还需要解决权重下溢、协方差正定性和并行通信开销。

---

## 十八、自测问题

1. 为什么单一 UKF 不能表示传播后弯曲或多峰的概率密度？
2. FPKE 和 CKE 分别对应什么类型的系统？
3. GMM 中 $`\boldsymbol\mu_i`$、$`\boldsymbol P_i`$ 和 $`w_i`$ 分别表示什么？
4. 为什么论文用无迹变换代替动力学 Jacobian？
5. “黑箱动力学”是否意味着完全不需要轨道传播模型？
6. 普通 GSF 与本文 AGMM 在无观测传播期间的主要区别是什么？
7. 为什么确定性系统中仍允许重新计算混合权重？
8. 二次规划中的 $`\boldsymbol M`$ 和 $`\boldsymbol N`$ 分别描述什么？
9. 为什么权重必须满足非负和归一化约束？
10. 六维 Gauss-Hermite 求积为什么容易出现维数灾难？
11. 本文是否会在传播过程中自动创建新的高斯分量？
12. 传播阶段权重更新与量测阶段权重更新的依据有何不同？
13. 为什么 EKF 在尾部观测下可能出现“误差大但方差小”？
14. 场景 III 中三种算法误差都很大的根本原因是什么？
15. 如果要将本文用于实际空间目标编目，还需要加入哪些机制？

---

## 简短答案

1. 单一 UKF 只保留均值和协方差，本质上仍是单高斯近似。
2. FPKE 对应连续时间随机系统，CKE 对应离散时间转移。
3. 分别表示局部中心、局部不确定性形状和全局混合系数。
4. 避免计算复杂动力学的 Jacobian，并可直接调用数值传播器。
5. 不是；仍需要能计算 $`\boldsymbol\phi(k,\boldsymbol x)`$ 的传播程序。
6. 普通 GSF 保持权重不变，AGMM 根据 FPKE/CKE 残差重估权重。
7. 这是对局部高斯近似误差的修正，不是物理概率质量转移。
8. $`\boldsymbol M`$ 描述新核之间重叠，$`\boldsymbol N`$ 描述新核与传播目标之间重叠。
9. 保证混合结果仍是合法概率密度。
10. 张量积点数随维数指数增长，例如三点六维规则需要 729 点。
11. 不会；论文预先放置 48 个零权重分量，传播时只改变参数和权重。
12. 前者依据概率密度演化方程，后者依据观测似然和 Bayes 公式。
13. 一阶线性化无法表达非线性后验形状，并可能严重低估协方差。
14. 仅角度观测缺少直接距离信息，几何可观测性很弱。
15. 过程噪声、模型参数不确定性、剪枝合并、自动分裂、数值稳定和并行调度。

---

## 参考文献

```bibtex
@article{Vishwajeet2014nonlinear,
  author  = {Vishwajeet, Kumar and Singla, Puneet and Jah, Moriba},
  title   = {Nonlinear Uncertainty Propagation for Perturbed Two-Body Orbits},
  journal = {Journal of Guidance, Control, and Dynamics},
  volume  = {37},
  number  = {5},
  pages   = {1415--1425},
  year    = {2014},
  doi     = {10.2514/1.G000472}
}
```

---

## 相关学习笔记

- [`概率初轨确定GMM学习笔记.md`](%E6%A6%82%E7%8E%87%E5%88%9D%E8%BD%A8%E7%A1%AE%E5%AE%9AGMM%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0.md)：用 GMM 表示短弧初轨确定中的可容许域；
- [`Automated Splitting Gaussian Mixture学习笔记.md`](Automated%20Splitting%20Gaussian%20Mixture%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0.md)：根据量测非线性在更新前自动分裂高斯分量；
- [`熵驱动AEGIS不确定性传播学习笔记.md`](%E7%86%B5%E9%A9%B1%E5%8A%A8AEGIS%E4%B8%8D%E7%A1%AE%E5%AE%9A%E6%80%A7%E4%BC%A0%E6%92%AD%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0.md)：根据非线性和信息指标自适应管理传播中的高斯分量。
