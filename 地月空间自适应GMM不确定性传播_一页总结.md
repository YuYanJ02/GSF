# 地月空间自适应 GMM 不确定性传播：一页总结与问题

> **论文**：*Application of Uncertainty Propagation with Adaptive Gaussian Mixture Models for Cislunar Objects*
>
> **作者**：John A. Gaebler 等；**会议**：AMOS 2025
>
> **原文**：[PDF](Application%20of%20Uncertainty%20Propagation%20with%20Adaptive%20Gaussian%20Mixture%20Models%20for%20Cislunar%20Objects.pdf)
>
> **新版详细笔记**：[研究思路、关键公式、自测题与答案](地月空间自适应GMM不确定性传播学习笔记.md)（参照本仓库概率初轨确定 GMM 学习笔记的结构）

## 1. 研究目标

地月多体动力学、长观测间隔及月球近接会使初始高斯不确定性变成弯曲、拉伸的非高斯分布，单高斯 EKF/UKF 预测难以表达。作者以多个局部高斯逼近整体概率密度，并设计**在线拆分与合并**：非线性强时增加分量，信息重复时减少分量，在精度与计算成本之间自适应平衡，尽量避免为每条轨道单独调参。本文验证的是**预测/传播阶段**，不是完整滤波器。

## 2. 方法主线

- **传播与拆分触发**：用无迹变换（UT）传播各分量，拟合最佳统计线性模型 $`y\approx Fx+b`$。残差协方差 $`P_e=P_y-FP_xF^T`$ 表示线性模型不能解释的变化，令 $`\epsilon=\mathrm{tr}(P_e)`$，比较两个有界指标：

```math
v_{\mathrm{MSE}}=\sqrt{\epsilon/\mathrm{tr}(P_y)},\qquad v_{\mathrm{EXP}}=\alpha^\gamma(1-e^{-\epsilon})^{1-\gamma},\qquad \gamma=0.
```

- **方向与拆分**：对协方差的各特征向量施加正负扰动，前向传播到 $`t_s+3\Delta t`$，选择二阶差分响应范数最大的方向；用三分量拆分库替换原分量，而非直接选择最大方差方向。
- **自适应合并**：用 Runnalls 的成对 KL 判别量上界选择合并对象，保持总权重、均值和二阶矩；搜索使混合熵上界最小的阈值 $`\mathcal D^*`$，再取 $`\mathcal D_s=(1-\Gamma_s)\mathcal D^*`$，以 $`\Gamma_s=0.9`$ 保守保留细节。
- **防止拆分失效**：依据同一步新子分量的内部合并代价进一步限制阈值，避免刚拆开的分量马上重新合并。

## 3. 算例与主要结果

采用 DE440、EGM2008 4×4、日月点质量引力及太阳光压；初始协方差来自角度观测最小二乘定轨并适当缩放。DRO、LTO、NRHO 分别传播 **13、5、19 天**，每例以 **10,000 粒子**作参考，不考虑过程噪声；统一使用拆分阈值 $`10^{-3}`$（MSE）、$`0.05`$（EXP）和上述合并参数。

- **DRO**：动力学平缓，末态仅需 7（MSE）/9（EXP）个分量；未关联粒子比例 NNA 为 0.18%/0.13%。
- **LTO**：拆分主要集中在近月末段；EXP 的似然一致性 LAM 更高，但 NNA 为 1.38%，略高于 MSE 的 1.20%。
- **NRHO**：两次近月过程使分量数增长，第二次近接后分布拉伸约 50,000 km；EXP 的 LAM 更高，但 NNA 为 6.84%，MSE 为 2.22%，最大合并 NISE 约为 0.015。

**结论**：EXP 通常更早、更频繁拆分，密度更精细；MSE 在 LTO/NRHO 中更宽、更保守。LAM 衡量密度重合，NNA 衡量观测关联覆盖，二者不能互相替代。统一参数的跨轨道表现支持方法的工程潜力，但强近月非线性仍是精度瓶颈。

## 4. 问题与后续研究

以下是阅读后提出的问题，不是论文已经证明的结论：

1. **尺度一致性**：六维位置/速度的迹如何缩放？EXP 直接使用 $`e^{-\epsilon}`$，改变单位或坐标会不会改变触发时机与阈值通用性？
2. **迟拆分与信息丢失**：作者指出重建 sigma 点、对称同方差拆分会丢失累计非线性；能否提前预判近月事件，或用偏度、多方向/异方差拆分改善尾部？
3. **合并依据**：熵上界最小不等于真实分布误差最小；$`\Gamma_s=0.9`$ 的可靠范围多大，能否根据 NISE 或局部非线性自动调节？
4. **运行级验证**：加入观测更新、过程噪声和低权重分量管理后，精度与耗时如何变化？还需完整滤波、参数敏感性及多初值统计试验。
