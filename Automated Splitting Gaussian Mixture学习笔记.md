# 自动分裂高斯混合非线性量测更新学习笔记

> **论文**：*Automated Splitting Gaussian Mixture Nonlinear Measurement Update*  
> **期刊**：Journal of Guidance, Control, and Dynamics, Vol. 41, No. 3, 2018, pp. 725–734  
> **DOI**：[10.2514/1.G003109](https://doi.org/10.2514/1.G003109)  
> **作者**：Kirsten Tuggle、Renato Zanetti（University of Texas at Austin）  
> **原文 PDF**：[Automated Splitting Gaussian Mixture Nonlinear.pdf](Automated%20Splitting%20Gaussian%20Mixture%20Nonlinear.pdf)  
> **出版信息核对**：[作者课题组期刊论文目录，第23项](https://sites.utexas.edu/near/publications/journal-publications/)  
>
> **排版约定**：行内公式使用 `` $`...`$ ``；块级公式使用独立的 `` ```math `` 代码块。每个公式块写成一行，编号用普通文字说明。本文公式以仓库中的25页原文 PDF 为依据。

---

## 一句话主旨

在非线性量测更新中，用先验不确定性与量测曲率共同选择高斯分量的分裂方向，再用后验权重平方加权的一阶/二阶 EKF 差异（KLD）决定是否继续分裂，以较少的高斯分量逼近弯曲或多峰后验。

---

## 一、论文要解决什么问题？

即使先验是高斯分布，经过非线性量测更新后，真实贝叶斯后验也可能成为“香蕉形”或多峰分布。单高斯 EKF 只给出均值和协方差，无法可靠表达这种形状。

高斯混合模型（GMM）可用多个局部高斯描述复杂分布，但分量越多，计算量越大。作者因此要回答两个问题：

1. **什么时候分裂？** 当前量测下，分裂带来的精度改善是否值得额外计算？
2. **沿哪个方向分裂？** 应该缩小哪个方向的局部协方差，才能有效降低线性化误差？

> 本文的目标不是仅让分裂后的先验“看起来更像原先验”，而是改善量测更新后对真实后验的近似。

### 研究范围：只处理量测更新

完整滤波周期包含动力学传播和量测更新，但本文只研究后者：

```math
p(x_{k-1}\mid Y^{k-1})\xrightarrow{\mathrm{propagation}}p(x_k\mid Y^{k-1})\xrightarrow{\mathrm{measurement\ update}}p(x_k\mid Y^k)
```

分裂在当前量测更新程序内部触发，**被分裂的是当前先验分量**。EKF/SEKF 的临时更新结果用于判断是否需要分裂；不能把同一量测反复当作新证据累积更新。

本文没有建立传播阶段的自适应分裂算法，也没有构造完整的传播、更新、剪枝和合并滤波器。

---

## 二、量测模型与高斯混合表示

原文式（2）采用标量量测：

```math
y=h(x)+\epsilon,\qquad x\in\mathbb R^n,\quad y\in\mathbb R,\quad \epsilon\sim\mathcal N(0,R),\quad R=\sigma_R^2
```

噪声与状态不相关：

```math
\mathbb E[\epsilon x^T]=0
```

某个先验分量为：

```math
x\sim\mathcal N(\bar x,P)
```

整体 GMM 先验为原文式（1）：

```math
p^-(x)=\sum_{i=1}^{N}w_i^-\mathcal N(x;\bar x_i,P_i),\qquad w_i^-\geq0,\quad\sum_iw_i^-=1
```

- 均值决定每个概率块的位置；
- 协方差决定该概率块的尺度和方向；
- 权重决定该概率块的概率质量。

**分裂并不是凭空降低整体先验不确定性。** 它用多个局部协方差更小、均值彼此错开的子分量重新近似父分量；子分量之间的均值分散继续承载原先验的一部分不确定性。

---

## 三、GMM 量测更新：内部状态与外部权重

### 1. 每个分量内部的状态更新

对第 $`i`$ 个分量，计算预测量测均值 $`\bar y_i`$、状态与量测互协方差 $`P_{xy,i}`$、创新方差 $`W_i=P_{yy,i}`$，再做局部更新：

```math
K_i=P_{xy,i}W_i^{-1},\qquad \hat x_i=\bar x_i+K_i(y-\bar y_i),\qquad P_i^+=P_i-K_iW_iK_i^T
```

对于非线性 $`h(x)`$，这些矩通常需要 EKF、UKF 等近似方法计算。

> **原文符号提醒**：仓库 PDF 式（5）的协方差更新中间项印为 $`P_i`$，按标准 LMMSE 公式及维数应为量测协方差 $`P_{yy,i}`$；本文使用上面的维数一致写法。

### 2. 不同分量之间的权重更新

第 $`i`$ 个分量对当前量测的近似预测似然为：

```math
\beta_i=\mathcal N(y;\bar y_i,W_i)
```

权重按贝叶斯规则重新归一化：

```math
\boxed{w_i^+=\frac{w_i^-\beta_i}{\sum_jw_j^-\beta_j}}
```

因此，某个分量能更好地解释当前量测，其后验权重就相对增加。分母涉及全部分量，所以权重更新不能完全孤立地进行。

最终近似后验为：

```math
p^+(x)\approx\sum_iw_i^+\mathcal N(x;\hat x_i,P_i^+)
```

**注意近似层次**：线性高斯量测下，分量后验仍为高斯；一般非线性量测下，真实分量后验通常不是高斯，上式是局部滤波后的高斯混合近似，不能理解为任意非线性问题的精确贝叶斯解。

---

## 四、一阶 EKF 与高斯二阶 EKF

令 $`\delta x=x-\bar x`$，在分量均值处定义：

```math
H=\left.\frac{\partial h}{\partial x}\right|_{\bar x},\qquad D=\left.\frac{\partial^2h}{\partial x\,\partial x^T}\right|_{\bar x}
```

其中标量量测的 $`H`$ 为 $`1\times n`$，$`D`$ 为 $`n\times n`$。

### 1. 一阶 EKF

忽略二阶及以上项：

```math
h(x)\approx h(\bar x)+H\delta x
```

预测量测均值与创新方差（原文式8）：

```math
\bar y_E=h(\bar x),\qquad W_E=HPH^T+R
```

量测更新：

```math
K_E=PH^TW_E^{-1},\qquad m_E=\bar x+K_E(y-\bar y_E),\qquad C_E=P-K_EW_EK_E^T
```

### 2. 高斯二阶 EKF（SEKF）

保留二次项：

```math
h(x)\approx h(\bar x)+H\delta x+\frac12\delta x^TD\delta x
```

对高斯先验，原文式（11）给出：

```math
\bar y_S=h(\bar x)+\frac12\mathrm{tr}(DP),\qquad W_S=HPH^T+R+\frac12\mathrm{tr}(DPDP)
```

更新仍采用线性残差修正形式：

```math
K_S=PH^TW_S^{-1},\qquad m_S=\bar x+K_S(y-\bar y_S),\qquad C_S=P-K_SW_SK_S^T
```

零均值高斯 $`\delta x`$ 的三阶中心矩为零，因此二次项不改变这里的状态与量测互协方差 $`PH^T`$。

### 3. 二阶修正说明了什么？

```math
\Delta\bar y=\frac12\mathrm{tr}(DP),\qquad \Delta W=\frac12\mathrm{tr}(DPDP)
```

两项都结合了量测曲率 $`D`$ 和先验范围 $`P`$。模型弯曲明显且分量覆盖范围大时，EKF 的一阶截断可能不可靠。分裂可缩小子分量的局部覆盖范围，从而减弱这种影响。

SEKF 在本文主要充当高阶参考，并不等于真实后验，也不保证在任意强非线性情况下准确。

---

## 五、分裂方向：同时考虑不确定性和非线性

### 1. 方向非线性

线性量测函数的 Jacobian 是常数；Jacobian 沿某方向变化越快，该方向的局部非线性越显著。原文式（20）至（22）得到：

```math
\left.\nabla_{\hat u}H(x)\right|_{\bar x}=D\hat u,\qquad \|D\hat u\|^2=\hat u^TD^TD\hat u
```

### 2. 方向不确定性

对单位方向 $`\hat u`$，原文式（23）定义：

```math
\sigma_{\hat u}^2=\frac{1}{\hat u^TP^{-1}\hat u}
```

它是固定所有正交方向后的条件方差，也等于协方差椭球沿该方向的径向尺度平方。**一般不等于投影的边缘方差** $`\hat u^TP\hat u`$；当方向为 $`P`$ 的特征向量时，二者一致。

### 3. 方向代价

原文式（24）联合两者：

```math
\boxed{J(\hat u)=\|D\hat u\|^2\sigma_{\hat u}^2=\frac{\hat u^TD^TD\hat u}{\hat u^TP^{-1}\hat u}}
```

它不会仅因为某方向方差最大就优先分裂。若该方向几乎没有量测非线性，其得分仍然较小；方差稍小但曲率很大的方向可能更值得细化。

### 4. 转化为特征值问题

取 $`P`$ 的对称正定平方根，令：

```math
v=P^{-1/2}\hat u,\qquad A=P^{1/2}D^TDP^{1/2}
```

则：

```math
J(v)=\frac{v^TAv}{v^Tv}\leq\lambda_{\max}(A)
```

取最大特征值对应的特征向量 $`v_{max}`$，再变换回原状态空间：

```math
\boxed{\hat u^*=\frac{P^{1/2}v_{\max}}{\|P^{1/2}v_{\max}\|}},\qquad J_{\max}=\lambda_{\max}(A)
```

**不要把 $`v_{max}`$ 直接当作原坐标中的分裂方向**：它属于上述变量变换后的坐标。若使用一般因子 $`P=LL^T`$，可等价地对 $`L^TD^TDL`$ 求特征向量，再通过 $`L`$ 映射回来。

特殊情况：

- $`D=0`$：量测在该展开点没有二阶曲率，该指标为零；这不排除更高阶非线性。
- $`P\propto I`$：方向选择主要由量测曲率决定。
- $`P`$ 奇异：原文的逆矩阵推导不能直接使用；应在非退化子空间处理，而不是未经说明直接代入。

---

## 六、三种“是否分裂”的候选判据

### 1. 最大方向代价

```math
\lambda_{\max}(A)\geq\tau_J
```

求方向时已得到该量，额外计算少；但阈值的容差含义较难直接解释。

### 2. 二阶创新方差贡献

原文式（30）为：

```math
\mathrm{tr}(DPDP)\geq\gamma HPH^T
```

即二阶贡献相对于一阶状态诱导量测方差足够大时分裂。SEKF 公式中的系数 $`1/2`$ 可吸收到比例参数 $`\gamma`$ 中。它主要比较创新方差，没有完整比较后验均值和协方差。

### 3. 一阶/二阶后验 KLD

定义 $`p_S=\mathcal N(m_S,C_S)`$、$`p_E=\mathcal N(m_E,C_E)`$，原文式（31）至（32）采用：

```math
D_{\mathrm{KL}}(p_S\|p_E)\geq\tau_{\mathrm{KL}}
```

两个非退化高斯之间的闭式公式：

```math
D_{\mathrm{KL}}(p_S\|p_E)=\frac12\left[\ln\frac{|C_E|}{|C_S|}-n+(m_E-m_S)^TC_E^{-1}(m_E-m_S)+\mathrm{tr}(C_SC_E^{-1})\right]
```

KLD 同时检查均值偏移和协方差失配。这里将 SEKF 作为参考，用 EKF 去近似它；参数顺序不能随意交换。

原文信息论定义是：

```math
D_{\mathrm{KL}}(p\|q)=\int p(x)\ln\frac{p(x)}{q(x)}\,dx
```

KLD 非负、相同分布时为零，但不对称，也不满足三角不等式，因此不是通常意义上的距离。自然对数对应单位 nat。

> 上述阈值符号在原文中有复用。本笔记将方向代价阈值和 KLD 阈值区分书写，不把它们当作同一单位、同一数值。

---

## 七、基于加权 KLD 的自动递归分裂

### 1. 平方权重判据

最终自动方案采用原文式（33）：

```math
\boxed{S_i=(w_{i,E}^+)^2D_{\mathrm{KL}}(p_{i,S}\|p_{i,E})\geq\tau}
```

其中 KLD 表示**局部近似差异**，EKF 更新后的权重表示**当前后验中的相对概率质量**。只有两者共同作用足够大时，才值得继续细化。

平方会更强地抑制小权重分量。例如，取阈值 $`\tau=0.1`$：

- 分量 A：$`w_A^+=0.8`$、KLD 为 $`0.3`$，得分 $`0.8^2\times0.3=0.192`$，触发分裂。
- 分量 B：$`w_B^+=0.1`$、KLD 为 $`5`$，得分 $`0.1^2\times5=0.05`$，不触发分裂。

即使 B 局部近似较差，对整体混合后验的影响也可能较小。原文的“penalizing the KLD check”是**降低检查得分**，不是添加负惩罚项。

平方权重是文中采用并观察到有利于停止的设计，不是整体混合 KLD 的严格分解公式；低权重也不等于在所有应用中都不重要。

### 2. 算法主线

以下是将论文递归思想整理成的操作流程，而非原文逐字伪代码：

```text
输入：当前时刻 GMM 先验、当前量测 y、量测函数 h、分裂模板、阈值
1. 对当前全部分量计算 EKF/SEKF 临时更新结果。
2. 用 EKF 预测似然全局归一化，得到各分量更新权重。
3. 计算每个分量的平方权重加权 KLD。
4. 若存在超阈值分量，对它计算最优方向并分裂其先验。
5. 替换父分量后，重新评估当前混合分布并归一化权重。
6. 重复上述过程，直到所有分量满足停止条件。
7. 返回最后一轮分量的 EKF 更新结果组成的 GMM 后验。
```

子分量的具体权重、均值偏移和协方差收缩来自已有一维分裂库。原文仿真使用参考文献[7]的库，算例Ⅰ每次将一个父分量替换为5个子分量。

若父分量概率为 $`w_i^-`$、模板归一化子权重为 $`a_j`$，则子分量的先验混合权重应为 $`w_i^-a_j`$，而不是把模板权重直接当成整个混合分布的权重。

---

## 八、用“最差可接受误差”确定 KLD 阈值

KLD 没有有限上界，因此不能简单取“最大 KLD 的某个百分比”。作者改用用户能够理解的标准化误差情形。

参考分布与可接受边界分布取为：

```math
p_1=\mathcal N(\mu,\Sigma),\qquad p_2=\mathcal N\!\left(\mu+c\Sigma^{1/2}v,\frac1k\Sigma\right),\qquad \|v\|=1,\ c>0,\ k>1
```

- $`c`$：允许的均值偏移，按参考分布的标准化尺度衡量。
- $`k`$：允许近似协方差低估的比例；协方差缩小到参考值的 $`1/k`$。

均值差具有马氏距离：

```math
(m_2-m_1)^T\Sigma^{-1}(m_2-m_1)=c^2
```

代入高斯 KLD 后得到原文式（35）：

```math
\boxed{\tau(c,k,n)=\frac12\left[n(k-\ln k-1)+c^2k\right]}
```

阈值不依赖具体 $`\mu`$、$`\Sigma`$ 或方向 $`v`$，可以预先计算。$`c`$ 越大、$`k`$ 越大，允许的偏差越大，阈值越高，分裂越宽松。

**说明性例子，不是论文仿真参数**：$`n=2,c=0.5,k=2`$ 时，阈值约为 $`0.5569`$ nat。$`k=2`$ 表示协方差减半，不是标准差减半。

该容差情形按单分量、权重为1推导；在实际混合判据中，平方权重进一步降低低概率分量的分裂优先级。因此它不是对整个混合后验真实误差的统一保证。

---

## 九、三个距离量测算例

原文采用二维位置状态与距离量测：

```math
h(x)=\sqrt{x^Tx}
```

在非零均值处，其 Hessian（原文式37）为：

```math
D(\bar x)=\frac1{\|\bar x\|}I-\frac{\bar x\bar x^T}{\|\bar x\|^3}
```

径向曲率为零，切向曲率非零：

```math
D(\bar x)\bar x=0,\qquad D(\bar x)v=\frac1{\|\bar x\|}v\quad\mathrm{if}\quad\bar x^Tv=0
```

因此距离量测的显著曲率方向是切向，而不一定是先验最大方差方向；这是方向选择比较的直观背景。

### 算例Ⅰ：香蕉形后验（图1至图3）

- 单高斯 EKF 无法表示后验曲率。
- 相对于真实后验的 KLD：EKF 为 **1.0461 nat**，本文 GMM 为 **0.3813 nat**。
- 相同阈值下，本文方向使用 **5个分量**，先验主轴方向使用 **9个分量**。

### 算例Ⅱ：先验没有明显主轴（图4至图6）

- 先验范围更大，且没有明显的最大不确定性方向。
- 本文方向使用 **17个分量**，主轴方向使用 **33个分量**。
- 本文结果对后验弯曲形状的表达更好；仅凭主轴选择初始方向容易具有任意性。

### 算例Ⅲ：双峰后验（图7至图9）

- 较大的距离量测产生两个明显分离的后验概率区域。
- EKF 的单高斯结果无法表达双峰。
- 本文方向使用 **21个分量**，主轴方向使用 **25个分量**。

这些结果支持本文方法在所示场景中用更少分量获得更好的后验表示，**不能据此认定在所有系统中都更准确或运行更快**。分量数比较使用相同停止阈值，也不等于比较达到了完全相同的真实后验误差。

---

## 十、核心认识与工程延伸

### 论文的核心认识

1. **分裂目标是改善后验，不只是约束先验分量尺寸。**
2. **方向选择同时需要量测非线性和先验不确定性。**
3. **是否继续分裂还要考虑该分量的后验概率质量。**
4. **局部高斯近似与全局非高斯表示可以结合。** 子分量内部使用 EKF，整体使用 GMM。

### 读者延伸问题：不属于论文已经证明的结论

- **高阶非线性漏判**：如果均值处 Hessian 很小，但分量范围内存在显著高阶项，方向代价和 EKF/SEKF KLD 是否同时失效？
- **尾部概率保持**：平方权重是否会过度压低碰撞概率、罕见轨道假设等小概率区域的优先级？
- **递归复杂度与终止**：高维、强非线性场景下，是否需要最大分量数、最大分裂深度和额外停止条件？
- **完整轨道滤波**：如何结合传播阶段分裂、向量量测、过程噪声、剪枝与合并？
- **工程验证**：需要进一步比较六维轨道多历元场景下的运行时间、真实后验误差及滤波一致性。

实现时还应采用对数似然归一化避免权重下溢，并用线性方程求解、Cholesky 分解和对数行列式计算 KLD，避免直接求逆或行列式引起数值问题。

---

## 十一、自测问题

1. 为什么高斯先验经过非线性量测后，后验不一定仍是高斯？
2. 本文在传播阶段还是量测更新阶段分裂？被分裂的是先验还是后验？
3. GMM 的分量内部更新与分量之间权重更新分别做什么？
4. $`\beta_i`$ 表示什么？为什么权重更新需要全部分量共同归一化？
5. 二阶 EKF 相比一阶 EKF，多了哪两个量测矩修正项？
6. 为什么用 $`D\hat u`$ 描述方向非线性？
7. $`1/(\hat u^TP^{-1}\hat u)`$ 与 $`\hat u^TP\hat u`$ 是否相同？
8. 为什么最大先验方差方向不一定是最佳分裂方向？
9. 求出 $`A`$ 的最大特征向量后，还需要怎样得到原坐标方向？
10. KLD 的两个参数能否交换？本文为何以 SEKF 为参考？
11. 为什么自动判据乘以后验权重的平方，而不是只检查局部 KLD？
12. 阈值中的 $`c`$ 和 $`k`$ 各表示什么？$`k=2`$ 是否意味着标准差减半？
13. KLD 检查通过，是否能保证接近真实贝叶斯后验？
14. 分裂是否降低整体先验不确定性？是否能把同一量测重复作为新证据？
15. 三个算例的分量数优势，是否能直接证明高维轨道系统中的运行时间优势？

---

## 简短答案

1. 非线性似然与高斯先验相乘后，通常不能保持高斯函数形式。
2. 量测更新阶段；利用临时更新结果判定，分裂当前先验分量。
3. 前者修正连续状态的均值和协方差，后者根据量测证据重新分配概率。
4. 分量预测量测似然；全概率公式给出归一化分母。
5. $`\frac12\mathrm{tr}(DP)`$ 和 $`\frac12\mathrm{tr}(DPDP)`$。
6. 它是 Jacobian 沿该方向的局部变化率。
7. 一般不同，分别对应条件方差和投影边缘方差；特征方向上相同。
8. 大方差方向可能接近线性，而另一个方向的量测曲率更显著。
9. 用 $`P^{1/2}`$ 映射回原坐标，再归一化。
10. 不能，KLD 不对称；SEKF 是包含二阶影响的参考近似。
11. 降低低概率分量优先级，避免局部误差驱动无效细化。
12. 标准化均值偏移和协方差低估比例；$`k=2`$ 对应标准差缩小为 $`1/\sqrt2`$。
13. 不能，它比较的是两个近似后验，并非与真实后验直接比较。
14. 不应改变原先验的概率含义；同一量测不能重复累积。
15. 不能，还需统计求导、特征分解、更新和分量管理的总耗时。

---

## 参考文献

### 本文

```bibtex
@article{Tuggle2018automated,
  author  = {Tuggle, Kirsten and Zanetti, Renato},
  title   = {Automated Splitting Gaussian Mixture Nonlinear Measurement Update},
  journal = {Journal of Guidance, Control, and Dynamics},
  volume  = {41},
  number  = {3},
  pages   = {725--734},
  year    = {2018},
  doi     = {10.2514/1.G003109}
}
```

### 原文参考文献[7]：分裂库与不确定性传播

```bibtex
@article{DeMars2013entropy,
  author  = {DeMars, Kyle J. and Bishop, Robert H. and Jah, Moriba K.},
  title   = {Entropy-Based Approach for Uncertainty Propagation of Nonlinear Dynamical Systems},
  journal = {Journal of Guidance, Control, and Dynamics},
  volume  = {36},
  number  = {4},
  pages   = {1047--1057},
  year    = {2013}
}
```

### 本仓库中的关联阅读

- [基于高斯混合的概率初轨确定学习笔记](%E6%A6%82%E7%8E%87%E5%88%9D%E8%BD%A8%E7%A1%AE%E5%AE%9AGMM%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0.md)：说明如何构造初始 GMM，并通过 GMUKF 保留与更新轨道假设。
- 两篇工作的分工不同：初轨确定笔记关注“初始概率分布如何构造和递推”，本文关注“当前量测下哪些高斯分量需要进一步细化”。
